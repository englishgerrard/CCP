#. PACKAGES
library(tidyverse)
library('terra')    # Core raster GIS data package
library('sf')       # Core vector GIS data package
library('raster')   # Older raster GIS package required by some packages
library('geodata')  # Data downloader

library('sp')        # Older vector GIS package - replaced by sf in most cases
library('rgdal')     # Interface to the Geospatial Data Abstraction Library
library('lwgeom')    # Lightweight geometry engine

library(lme4)
